// Inicializar AOS cuando la página se cargue
document.addEventListener("DOMContentLoaded", function () {
    AOS.init({
        duration: 1000,  // Duración de la animación (en milisegundos)
        once: true       // Para que la animación ocurra solo una vez
    });
});
